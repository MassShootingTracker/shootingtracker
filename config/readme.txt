Config is loaded as a JSON file. The default config file is 'default.json'.

However, the presense of a 'local.json' file will be used in preference to default.json.

Never commit your local.json file, or any sensitive keys contained within it.